컬렉션 프레임워크

Set : 집합
	- 데이터의 중복을 허용하지 않는다.
	- 데이터의 순서를 보장하지 않는다.

구현 클래스
	-HashSet
		Set 인터페이스를 구현한 가장 대표적인 클래스
		중복되는 값을 저장하면 저장되지 않는다. 인덱스가 존재하지 않고, 순서를 보쟝하지 않는다.
		인덱스 번호로 값을 가져오는 게 불가능하다.
		해시코드를 이용하여 데이터를 처리하기에 상대적으로 빠르다.
		위의 특징을 활용하여 값의 존재 여부를 파악할 때 사용한다.(검색)

	-순서 부여 : iterator()